package com.lms.model.enums;

/**
 * Hierarchical sub-roles assigned to teachers by the admin.
 * These model the real school hierarchy.
 */
public enum SubRole {
    NONE,
    PRINCIPAL,
    DEPUTY_PRINCIPAL,
    DEPARTMENT_HEAD,
    SENIOR_TEACHER,
    CLASS_TEACHER
}
